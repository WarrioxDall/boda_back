/**
 * Icono para "Lluvia de sobres".
 *
 * Para cambiarlo:
 * - SVG: edita LluviaSobresIcon.jsx o reemplaza el export aquí.
 * - Emoji: pon type: 'emoji' y el carácter en value.
 * - Imagen: pon type: 'image' y la ruta en value.
 */
import LluviaSobresIcon from '../icons/LluviaSobresIcon';

export const lluviaSobresIcon = {
  type: 'svg',
  value: LluviaSobresIcon,
  className: 'inline-block h-9 w-9 align-[-4px]',
};
